library(cells) {
	cell(BUF) { /* 74LVC1G34GW (N=1, pd=2.0ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(Y) { direction: output; function: "A"; }
	}
	cell(NOT) { /* 74LVC1G240GW (N=1, pd=1.9ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(Y) { direction: output; function: "!A"; }
	}
	cell(MUX2) { /* 74LVC1G157GW (N=1, pd=2.7ns, TSSOP6) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(S) { direction: input; }
		pin(Y) { direction: output; function: "A*!S+B*S"; }
	}
	cell(MUX2_4) { /* 74LVC157APW (N=1, pd=2.7ns, TSSOP16) */
		area: 2;
		pin(A1) { direction: input; }
		pin(B1) { direction: input; }
		pin(A2) { direction: input; }
		pin(B2) { direction: input; }
		pin(A3) { direction: input; }
		pin(B3) { direction: input; }
		pin(A4) { direction: input; }
		pin(B4) { direction: input; }
		pin(S) { direction: input; }
		pin(Y1) { direction: output; function: "(A1*B1)+(S*B1)+(!S*A1)"; }
		pin(Y2) { direction: output; function: "(A2*B2)+(S*B2)+(!S*A2)"; }
		pin(Y3) { direction: output; function: "(A3*B3)+(S*B3)+(!S*A3)"; }
		pin(Y4) { direction: output; function: "(A4*B4)+(S*B4)+(!S*A4)"; }
	}
	cell(XOR) { /* 74LVC1G86GW (N=1, pd=2.3ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(Y) { direction: output; function: "(A*!B)+(!A*B)"; }
	}
	cell(AND) { /* 74LVC1G08GW (N=1, pd=2.1ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(Y) { direction: output; function: "(A*B)"; }
	}
	cell(OR) { /* 74LVC1G32GW (N=1, pd=2.1ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(Y) { direction: output; function: "(A+B)"; }
	}
	cell(XNOR) { /* 74AUP1T87GW (N=1, pd=2.8ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(Y) { direction: output; function: "!((A*!B)+(!A*B))"; }
	}
	cell(NAND) { /* 74LVC1G00GW (N=4, pd=2.2ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(Y) { direction: output; function: "!(A*B)"; }
	}
	cell(NOR) { /* 74LVC1G02GW (N=1, pd=2.1ns, TSSOP5) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(Y) { direction: output; function: "!(A+B)"; }
	}
	cell(XOR3) { /* 74LVC1G386GW (N=1, pd=4.5ns, TSSOP6) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(Y) { direction: output; function: "(A*B*C)+(A*!B*!C)+(!A*B*!C)+(!A*!B*C)"; }
	}
	cell(AND3) { /* 74LVC1G11GW (N=1, pd=2.6ns, TSSOP6) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(Y) { direction: output; function: "(A*B*C)"; }
	}
	cell(OR3) { /* 74LVC1G332GW (N=1, pd=2.6ns, TSSOP6) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(Y) { direction: output; function: "(A+B+C)"; }
	}
	cell(NAND3) { /* 74LVC1G10GW (N=1, pd=2.6ns, TSSOP6) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(Y) { direction: output; function: "!(A*B*C)"; }
	}
	cell(NOR3) { /* 74LVC1G27GW (N=1, pd=2.6ns, TSSOP6) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(Y) { direction: output; function: "!(A+B+C)"; }
	}
	cell(AND4) { /* 74HCS21PW (N=2, pd=10ns, TSSOP14) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(D) { direction: input; }
		pin(Y) { direction: output; function: "(A*B*C*D)"; }
	}
	cell(OR4) { /* 74LVC32APW (N=1*, pd=5ns, TSSOP14, daisy-chained)
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(D) { direction: input; }
		pin(Y) { direction: output; function: "(A+B+C+D)"; }
	}
	cell(NAND4) { /* 74AHCT30PW (N=1*, pd=5ns, TSSOP14, 8-input) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(D) { direction: input; }
		pin(Y) { direction: output; function: "!(A*B*C*D)"; }
	}
	cell(NOR4) { /* 74HC4002PW (N=2, pd=11ns, TSSOP14) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(D) { direction: input; }
		pin(Y) { direction: output; function: "!(A+B+C+D)"; }
	}
	cell(NAND8) { /* 74AHCT30PW (N=1, pd=5ns, TSSOP14) */
		area: 2;
		pin(A) { direction: input; }
		pin(B) { direction: input; }
		pin(C) { direction: input; }
		pin(D) { direction: input; }
		pin(E) { direction: input; }
		pin(F) { direction: input; }
		pin(G) { direction: input; }
		pin(H) { direction: input; }
		pin(Y) { direction: output; function: "!(A*B*C*D*E*F*G*H)"; }
	}
	cell(MUX8) { /* CD74AC151PWR (N=1, pd=6.6ns, TSSOP16) */
		area: 2;
		pin(D1) { direction: input; }
		pin(D2) { direction: input; }
		pin(D3) { direction: input; }
		pin(D4) { direction: input; }
		pin(D5) { direction: input; }
		pin(D6) { direction: input; }
		pin(D7) { direction: input; }
		pin(D8) { direction: input; }
		pin(S1) { direction: input; }
		pin(S2) { direction: input; }
		pin(S3) { direction: input; }
		pin(Y) { direction: output; function: "D1*!S3*!S2*!S1+D2*!S3*!S2*S1+D3*!S3*S2*!S1+D4*!S3*S2*S1+D5*S3*!S2*!S1+D6*S3*!S2*S1+D7*S3*S2*!S1+D8*S3*S2*S1"; }
	}
	cell(DEMUX4) { /* 74LVC139PW (N=2, pd=2.5ns, TSSOP16) */
		area: 2;
		pin(INn) { direction: input; }
		pin(S1) { direction: input; }
		pin(S2) { direction: input; }
		pin(Y1n) { direction: output; function: "INn+(S1+S2)"; }
		pin(Y2n) { direction: output; function: "INn+(!S1+S2)"; }
		pin(Y3n) { direction: output; function: "INn+(S1+!S2)"; }
		pin(Y4n) { direction: output; function: "INn+(!S1+!S2)"; }
	}
	cell(D_FF) { /* 74LVC1G79GW (N=1, pd=2.2ns, TSSOP5) */
		area: 2;
		ff("Q") {
			next_state: "(D)";
			clocked_on: "CLK";
		}
		pin(D) { direction: input; }
		pin(CLK) { direction: input; }
		pin(Q) { direction: output; function: "Q"; }
	}
	cell(D_FF_R) { /* 74LVC1G175GW (N=1, pd=3.1ns, TSSOP6) */
		area: 2;
		ff("Q") {
			next_state: "(D)";
			clocked_on: "CLK";
			clear: "CLRn";
		}
		pin(D) { direction: input; }
		pin(CLK) { direction: input; }
		pin(CLRn) { direction: input; }
		pin(Q) { direction: output; function: "Q"; }
	}
	cell(D_FF_SR) { /* 74LVC1G74DP (N=1, pd=3.5ns, TSSOP8) */
		area: 2;
		ff("Q") {
			next_state: "(D)";
			clocked_on: "CLK";
			preset: "PREn";
			clear: "CLRn";
		}
		pin(D) { direction: input; }
		pin(CLK) { direction: input; }
		pin(PREn) { direction: input; }
		pin(CLRn) { direction: input; }
		pin(Q) { direction: output; function: "Q"; }
		pin(Qn) { direction: output; function: "!Q"; }
	}
}
